[pytest]
pythonpath = .
testpaths = tests
# Chapter 13: quarantined tests are skipped by the main run
addopts = -m "not quarantine"
markers =
    quarantine(reason): flaky test with an owner and a ticket
